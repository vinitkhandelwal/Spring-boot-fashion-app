--alter table productlenders add column subcategory varchar(255);
--alter table productlenders add column gender varchar(10);
--alter table productlenders add column size varchar(100);
--alter table productlenders add column fit varchar(50);
--alter table productlenders add column coordinates point;
--
--
--CREATE TABLE public.productrented (
--	id int8 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
--	productlenderId int8 NOT NULL,
--	renterUserId int8 not null,
--	dateFrom DATE NOT null,
--	dateTo Date NOT null,
--	CONSTRAINT fk_productlenderid
--          FOREIGN KEY(productlenderid)
--    	  REFERENCES productlenders(id),
--    CONSTRAINT fk_productrenteruserid
--          FOREIGN KEY(renterUserId)
--    	  REFERENCES users(id),
--	CONSTRAINT productrentedkey PRIMARY KEY (id)
--);

--alter table productlenders rename category to type;
--alter table productlenders rename subcategory to category;
